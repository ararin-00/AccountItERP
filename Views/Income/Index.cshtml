@model List<AccountItERP.Models.Income>

@{
    ViewData["Title"] = "Income Management";
}

@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" />
}

<div class="user-management-page">

    <div class="page-title d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2>Income Management</h2>
            <p>Track and manage all income records.</p>
        </div>

        <button type="button"
                class="btn btn-primary px-4"
                data-bs-toggle="modal"
                data-bs-target="#createIncomeModal">
            + Add Income
        </button>
    </div>

    <div class="dashboard-card">

    <form asp-action="Index"
          method="get"
          class="row g-3 mb-4 align-items-center">

        <div class="col-md-9">
            <input type="text"
                   name="search"
                   value="@ViewBag.Search"
                   class="form-control"
                   placeholder="Search income source..." />
        </div>

        <div class="col-md-3 d-flex gap-2">
            <button type="submit"
                    class="btn btn-primary flex-fill">
                Search
            </button>

            @if (!string.IsNullOrWhiteSpace(ViewBag.Search))
            {
                <a asp-action="Index"
                   class="btn btn-outline-secondary">
                    Clear
                </a>
            }
        </div>

    </form>

        <div class="table-responsive">
            <table class="table align-middle user-table">
                <thead>
                    <tr>
                        <th>Source</th>
                        <th>Amount</th>
                        <th>Date Received</th>
                        <th>Description</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var income in Model)
                    {
                        <tr>
                            <td><strong>@income.IncomeSource</strong></td>

                            <td>
                                <span class="amount-income">
                                    ₱@income.Amount.ToString("N2")
                                </span>
                            </td>

                            <td>@income.DateReceived.ToString("MMM dd, yyyy")</td>

                            <td>@income.Description</td>

                            <td>
                                <div class="d-flex justify-content-end gap-2">
                                    <button type="button"
                                            class="btn btn-outline-primary btn-sm px-3"
                                            data-bs-toggle="modal"
                                            data-bs-target="#editIncomeModal-@income.IncomeID">
                                        Edit
                                    </button>

                                    <a href="#"
                                    class="btn btn-outline-danger btn-sm px-3 delete-btn"
                                    data-url="@Url.Action("Delete", new { id = income.IncomeID })">
                                        Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (ViewBag.TotalPages > 1)
        {
            <div class="pagination-wrapper">
                <div class="pagination-info">
                    Page @ViewBag.CurrentPage of @ViewBag.TotalPages
                </div>

                <nav>
                    <ul class="pagination custom-pagination mb-0">

                        <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                            <a class="page-link"
                               asp-action="Index"
                               asp-route-page="@(ViewBag.CurrentPage - 1)"
                               asp-route-search="@ViewBag.Search">
                                Previous
                            </a>
                        </li>

                        @for (int i = 1; i <= ViewBag.TotalPages; i++)
                        {
                            <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                                <a class="page-link"
                                   asp-action="Index"
                                   asp-route-page="@i"
                                   asp-route-search="@ViewBag.Search">
                                    @i
                                </a>
                            </li>
                        }

                        <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
                            <a class="page-link"
                               asp-action="Index"
                               asp-route-page="@(ViewBag.CurrentPage + 1)"
                               asp-route-search="@ViewBag.Search">
                                Next
                            </a>
                        </li>

                    </ul>
                </nav>
            </div>
        }

    </div>

</div>

<!-- CREATE MODAL -->
<div class="modal fade"
     id="createIncomeModal"
     tabindex="-1"
     aria-labelledby="createIncomeModalLabel"
     aria-hidden="true">

    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content custom-modal">

            <form asp-action="Create"
            method="post"
            class="confirm-form"
            data-confirm-type="create">
                @Html.AntiForgeryToken()

                <div class="modal-header">
                    <div>
                        <h5 class="modal-title" id="createIncomeModalLabel">
                            Add Income
                        </h5>
                        <p class="modal-subtitle">
                            Create a new income record.
                        </p>
                    </div>

                    <button type="button"
                            class="btn-close"
                            data-bs-dismiss="modal"
                            aria-label="Close">
                    </button>
                </div>

                <div class="modal-body">

                    <div class="mb-3">
                        <label class="form-label fw-bold">Income Source</label>
                        <input type="text"
                               name="IncomeSource"
                               class="form-control"
                               placeholder="Enter income source"
                               required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Amount</label>
                        <input type="number"
                               name="Amount"
                               step="0.01"
                               min="0"
                               class="form-control"
                               placeholder="Enter amount"
                               required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Date Received</label>
                        <input type="date"
                               name="DateReceived"
                               class="form-control"
                               required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Description</label>
                        <textarea name="Description"
                                  class="form-control"
                                  rows="3"
                                  placeholder="Enter description"></textarea>
                    </div>

                </div>

                <div class="modal-footer">
                    <button type="button"
                            class="btn btn-outline-primary px-4"
                            data-bs-dismiss="modal">
                        Cancel
                    </button>

                    <button type="submit"
                            class="btn btn-primary px-4">
                        Save Income
                    </button>
                </div>

            </form>

        </div>
    </div>
</div>

<!-- EDIT MODALS - OUTSIDE TABLE -->
@foreach (var income in Model)
{
    <div class="modal fade"
         id="editIncomeModal-@income.IncomeID"
         tabindex="-1"
         aria-labelledby="editIncomeModalLabel-@income.IncomeID"
         aria-hidden="true">

        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content custom-modal">

                <form asp-action="Edit"
                method="post"
                class="confirm-form"
                data-confirm-type="edit">
                    @Html.AntiForgeryToken()

                    <input type="hidden" name="IncomeID" value="@income.IncomeID" />
                    <input type="hidden" name="UserID" value="@income.UserID" />

                    <div class="modal-header">
                        <div>
                            <h5 class="modal-title" id="editIncomeModalLabel-@income.IncomeID">
                                Edit Income
                            </h5>
                            <p class="modal-subtitle">
                                Update this income record.
                            </p>
                        </div>

                        <button type="button"
                                class="btn-close"
                                data-bs-dismiss="modal"
                                aria-label="Close">
                        </button>
                    </div>

                    <div class="modal-body">

                        <div class="mb-3">
                            <label class="form-label fw-bold">Income Source</label>
                            <input type="text"
                                   name="IncomeSource"
                                   value="@income.IncomeSource"
                                   class="form-control"
                                   required />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Amount</label>
                            <input type="number"
                                   name="Amount"
                                   value="@income.Amount"
                                   step="0.01"
                                   min="0"
                                   class="form-control"
                                   required />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Date Received</label>
                            <input type="date"
                                   name="DateReceived"
                                   value="@income.DateReceived.ToString("yyyy-MM-dd")"
                                   class="form-control"
                                   required />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Description</label>
                            <textarea name="Description"
                                      class="form-control"
                                      rows="3">@income.Description</textarea>
                        </div>

                    </div>

                    <div class="modal-footer">
                        <button type="button"
                                class="btn btn-outline-primary px-4"
                                data-bs-dismiss="modal">
                            Cancel
                        </button>

                        <button type="submit"
                                class="btn btn-primary px-4">
                            Save Changes
                        </button>
                    </div>

                </form>

            </div>
        </div>
    </div>
}